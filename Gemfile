source "https://rubygems.org"

# This is the gem version GitHub Pages uses. Keeping it locked here means
# your local builds match what GitHub will actually serve.
gem "github-pages", group: :jekyll_plugins
